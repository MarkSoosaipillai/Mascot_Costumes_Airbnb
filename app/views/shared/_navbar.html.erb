<div class="navbar navbar-expand-sm navbar-lewagon">
  <div class="container-fluid">
    <%= link_to root_path do %>
    <%= image_tag "chinese costume.jpg", class: 'logo' %>
    <% end %>

  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
   </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
      <% if user_signed_in? %>
      <li class="nav-item">Welcome, <strong><%= current_user.name.upcase %></strong></li>
        <li class="nav-item">
          <%= link_to "Add Costume", new_costume_path, class:"nav-link p-1 ms-5" %>
        </li>
        <li class="nav-item">
          <%= link_to "My Rentals", user_reservations_path(@user), class:"nav-link p-1 ms-5" %>
        </li>


        <li class="nav-item">
          <% if current_page?(root_path)%>
            <%= link_to "Browse all Costumes", costumes_path, class:"nav-link underline p-1 ms-5"  %>
          <% else %>
            <%= link_to "Browse all Costumes", costumes_path, class:"nav-link p-1 ms-5" %>
          <% end %>
        </li>

          <li>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, class:"nav-link p-1 ms-5"  %>
          </li>
        <% else %>
          <li>
            <%= link_to "Login", user_session_path, class:"nav-link p-1 ms-5" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
